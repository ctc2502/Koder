void setup(){
  size(400,400);
  fill(24,65,97);
  rect(46,90,77,85);
}


void draw(){
String[] cards = {"hjerte", "kløver", "ruder", "spar"};

int index = int(random(cards. length));

println(cards[index]);

  String[] Numb = {"Es", "2", "3", "4", "5", "6", "7", "8", "9", "10", "Knægt", "Droning", "Konge"};

int index2 = int(random(Numb. length));

println(Numb[index2]);

for (int i = 0; i < ; i = i+1);{
break;
}

}
